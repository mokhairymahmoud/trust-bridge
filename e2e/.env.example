# E2E Environment Configuration
#
# Copy this file to .env and set the values.
# The E2E_DECRYPTION_KEY is generated by the encrypt command.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your encryption key from the encrypt step
#   docker compose up --build -d

# Decryption key (64 hex characters) - generated by trustbridge encrypt
E2E_DECRYPTION_KEY=

# Contract ID for testing (default: contract-allow)
TB_CONTRACT_ID=contract-allow

# Asset ID (must match the asset ID used during encryption)
TB_ASSET_ID=tb-asset-e2e-001
